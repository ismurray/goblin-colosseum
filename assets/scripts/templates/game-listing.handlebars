{{#each games as |game|}}

 <ul data-id="{{game.id}}">
  <li>Game ID: {{game.id}}</li>
  <li>Is the game over?: {{game.over}}</li>
  <li>Score: {{game.score}}</li>
  <li>Round: {{game.round}}</li>
  <li>Player state:
    {{#if game.player_state}}
      {{game.player_state}}
    {{else}}
      No player_state listed yet.
    {{/if}}
  </li>
  <li>Goblin states:
    {{#if game.goblin_state}}
      {{game.goblin_state}}
    {{else}}
      No player_state listed yet.
    {{/if}}
  </li>
  {{#unless game.over}}
  <button type="button" class="load-button" data-id="{{game.id}}">Load Game</button>
  {{/unless}}
  <ul>
    <button type="button" class="delete-button" data-id="{{game.id}}">Delete Game</button>
  </ul>
</ul>

{{/each}}
